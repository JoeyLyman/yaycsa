# Environment variables are split per app:
#
# apps/server/.env    — Vendure server (DB, superadmin, cookie secret)
# apps/storefront/.env — SvelteKit frontend (Vendure API URLs)
#
# See each app's .env for details. Copy .env.example → .env in each app directory.
